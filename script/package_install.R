# install all necessary packages 

# package list in scripts/analysis.R
pkgs <- c('tidyr',
          'dplyr',
          'ggplot2',
          'nlme',
          'readxl')

# install packages
install.packages(pkgs)